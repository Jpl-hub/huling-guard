# 云端资源封存清单

## 1. 封存目的

本文记录释放 ECS 前已经从云端保留下来的项目资产。文档不包含服务器密码、SSH 私钥、公网 IP 或任何云账号敏感信息，只记录后续恢复项目时需要知道的目录、文件类型和恢复顺序。

本次封存时间：`2026-04-12 22:28 Asia/Shanghai`

## 2. 云端运行状态快照

释放前的运行状态：

- 操作系统：Ubuntu 22.04
- 运行目录：`/opt/huling-guard/app`
- 容器：`huling-guard-runtime`
- 运行端口：容器 `8000` 映射到主机 `18014`
- Docker Compose：`2.40.3`
- 运行镜像：`app-huling-guard-runtime:latest`
- 运行镜像大小：约 `9.3GB`
- 应用目录总量：约 `70M`

## 3. 已拉回本地的资产

本地封存目录：

```text
_local_backups/ecs-final-20260412
```

该目录已加入 `.gitignore`，不会推送到 GitHub。它是本机恢复用的本地备份，不是开源仓库内容。

封存结果：

| 目录 | 大小 | 价值 |
| --- | ---: | --- |
| `_local_backups/ecs-final-20260412/runtime-release` | 约 `19M` | 运行时模型发布包、配置、发布清单和验证报告 |
| `_local_backups/ecs-final-20260412/runtime-demo` | 约 `47M` | 模拟监看视频、上传视频、标绘视频、预测结果和样例报告 |
| `_local_backups/ecs-final-20260412/runtime-data` | 约 `144K` | 历史留档数据库、会话 JSON 和 Markdown 报告 |
| `_local_backups/ecs-final-20260412/Dockerfile.runtime` | 小文件 | 运行时镜像构建文件 |
| `_local_backups/ecs-final-20260412/docker-compose.runtime.yml` | 小文件 | 运行时容器编排文件 |
| `_local_backups/ecs-final-20260412/build.log` | 小文件 | 云端构建日志 |
| `_local_backups/ecs-final-20260412/rebuild.log` | 小文件 | 云端重建日志 |

封存文件数：`133` 个。

## 4. 关键文件校验

本地备份目录内已经生成：

```text
_local_backups/ecs-final-20260412/MANIFEST.txt
_local_backups/ecs-final-20260412/SHA256SUMS.txt
```

关键发布文件校验值：

```text
f15ad05c573c7c9fa85413e15a67633f8170f6ab5bc9e38fa1acf4b11b584987  runtime-release/checkpoints/selected.pt
af3c934d4c4a09791f7ef129fb8f86be3e5d1b237d5b27adb71612dd04b8406a  runtime-release/release_manifest.json
f7463e9a809764130da38fc5f412a32769872f9b168537045e4bbf3359273938  runtime-release/config/runtime_config.yaml
```

## 5. 历史留档资产

历史留档目录：

```text
runtime-data/archive/
```

其中包括：

```text
runtime-data/archive/runtime_sessions.sqlite3
runtime-data/archive/sessions/*.json
runtime-data/archive/sessions/*.md
```

已封存的会话报告包括 8 组 JSON/Markdown 文件，时间跨度为 `2026-04-10` 到 `2026-04-11`。这些文件用于恢复历史回看、交接班摘要和事件复核。

## 6. 上传与标绘视频资产

视频与推理结果保存在：

```text
runtime-demo/videos/
runtime-demo/annotated/
runtime-demo/predictions/
runtime-demo/reports/
runtime-demo/uploads/jobs/
```

其中：

- `videos/`：原始模拟视频和上传视频。
- `annotated/`：带算法标绘层的视频。
- `predictions/`：逐帧或逐窗口预测结果。
- `reports/sessions/`：会话级 JSON 报告。
- `reports/markdown/`：会话级 Markdown 报告。
- `uploads/jobs/`：上传分析任务状态。

这些内容不建议直接提交 GitHub，因为包含视频和运行产物；需要时从本地 `_local_backups` 恢复。

## 7. 后续恢复步骤

如果后续重新开一台 ECS 或本地 Docker 环境，可以按以下顺序恢复：

1. 从 GitHub 拉取最新代码。
2. 构建前端：`cd frontend && npm install && npm run build`。
3. 从 `_local_backups/ecs-final-20260412/` 拷回以下目录：

```bash
cp -a _local_backups/ecs-final-20260412/runtime-release ./runtime-release
cp -a _local_backups/ecs-final-20260412/runtime-demo ./runtime-demo
cp -a _local_backups/ecs-final-20260412/runtime-data ./runtime-data
```

4. 启动运行时：

```bash
docker compose -f docker-compose.runtime.yml up --build
```

5. 验证接口：

```bash
curl -fsS http://127.0.0.1:18014/health
curl -fsS "http://127.0.0.1:18014/archives?limit=300"
```

6. 打开控制台检查：

```text
http://127.0.0.1:18014/dashboard
```

## 8. 释放云资源前的最终确认

释放 ECS 前建议确认：

- GitHub 已推送最新代码和文档。
- 本地 `_local_backups/ecs-final-20260412` 存在且大小约 `66M`。
- `MANIFEST.txt` 和 `SHA256SUMS.txt` 已生成。
- 服务器上没有唯一副本。
- 不需要继续保留公网访问。

确认以上条件后，可以释放 CPU 服务器资源。后续重新部署时，本地备份加 GitHub 仓库即可恢复 1.0 运行状态。
